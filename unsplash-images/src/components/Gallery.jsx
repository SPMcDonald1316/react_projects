import { useQuery } from '@tanstack/react-query';

const url = `https://api.unsplash.com/search/photosclient_id=${
  import.meta.env.VITE_API_KEY
}`;

const Gallery = () => {
  return <h2>Gallery</h2>;
};
export default Gallery;
